<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="AppTheme" parent="AppTheme.Base">
    </style>
    <!-- Base theme applied no matter what API -->
    <style name="AppTheme.Base" parent="Theme.AppCompat.Light">
        <!--<item name="android:windowNoTitle">true</item>-->
        <!--We will be using the toolbar so no need to show ActionBar-->
        <!--<item name="windowActionBar">true</item>-->
        <!-- Set theme colors from http://www.google.com/design/spec/style/color.html#color-color-palette-->
        <!-- colorPrimary is used for the default action bar background -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <!-- colorPrimaryDark is used for the status bar -->
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <!-- colorAccent is used as the default value for colorControlActivated
             which is used to tint widgets -->
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColorPrimary">@color/textColorPrimary</item>
        <item name="android:textColorSecondary">@color/textColorSecondary</item>
        <item name="android:divider">@color/divider</item>

        <!-- You can also set colorControlNormal, colorControlActivated
            colorControlHighlight and colorSwitchThumbNormal. -->
    </style>

    <style name="MenuItemRippleStyle" parent="Material.Drawable.Ripple.Wave">
        <item name="rd_rippleColor">#ffffa707</item>
        <item name="rd_rippleAnimDuration">80</item>
        <item name="rd_maskType">rectangle</item>
        <item name="rd_cornerRadius">0dp</item>
        <item name="spn_dividerAnimDuration">10</item>"
    </style>

    <style name="EditTextMine" parent="Base.TextAppearance.AppCompat.Caption">
        <item name="android:textSize">15sp</item>
        <item name="android:textColor">?android:textColorSecondary</item>
    </style>
    <style name="TextAppearanceMine" parent="Base.TextAppearance.AppCompat.Body1">
        <item name="android:textSize">17sp</item>
    </style>
</resources>